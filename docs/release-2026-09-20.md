# Expedition design and multiplayer fixes

Built on main commit 4f60e8b. Preserves its 149 enabled locations, Leaflet map, photo inspection controls, bot and pass-and-play modes, and longer match settings.

- Redesigned home with destination cards, focused play actions, and map selection.
- Added Quick Escape: five unique places, with a world wildcard finale for SA × NL. Existing saved length settings are retained.
- Country and optional city selection; small maps use their available unique locations.
- Host revisions now own online state. Local pin movement cannot block incoming snapshots; stale locks and continue packets are scoped to their question. Locks retry until acknowledged. Joiners no longer create competing hosts. Host starts explicitly after both players join.
- Browser-session peer identities and host snapshots support refreshing within a room. Private browsing may disable this recovery.
- Fixed hotseat timing and timeout handoffs, bot/hotseat rematches, invalid/late pins, zero-time origins, and unsubmitted timeout scoring (zero).
- Added photo zoom/reset controls; bounded panning and isolated photo keyboard controls from map inputs.
- Fixed relay pagination, database cold-start initialization, retention, per-peer fallback, and visible connection failures.

## Verification

- `npm run test:game`: 76 tests passed, including five-question online state progression, rematch, stale messages, timer and hotseat regressions.
- `npm run typecheck`: passed.
- `npm run build`: passed, Vercel output generated. Local DB migration skipped because DATABASE_URL is unset.
- Browser: inspected desktop homepage, created/joined a room in two tabs, verified shared question and synchronized timeout reveal. Full production multiplayer and mobile testing remain outstanding.
- The inherited `npm test` Grok-template suite has 16 failures involving unavailable .grok template files and platform metadata expectations. They are separate from `test:game`; not suppressed.

## Remaining constraints

The fetched main contains 149 enabled places, not the 310 additions reported in chat; no absent data was fabricated. The external photo catalog includes broken Commons URLs (confirmed Bo-Kaap 404). Photo repair is still pending; retry controls do not replace missing source files.

Online games remain host-authoritative private rooms, not ranked/anti-cheat secure play. Answers can be inferred from the shipped location catalog. Production relay reliability across Vercel instances requires the existing DATABASE_URL configuration. In-memory relay storage is for single-instance development and is not durable.
